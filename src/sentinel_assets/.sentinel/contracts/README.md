# Contracts (versioned)
- Name contracts as <domain>.vN.(yaml|json).
- Bump version on breaking change; never silently edit a version.
- Put sample payloads in `fixtures/<contract>/...` for consumer tests.
