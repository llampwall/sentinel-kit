# ProducedBy=API RulesHash=API@1.0 Decision=D-0002
contract: users.v1
description: List of user records for read operations.
schema:
  type: array
  items:
    type: object
    required: [id, email, created_at]
    properties:
      id: { type: string, format: uuid }
      email: { type: string, format: email }
      created_at: { type: string, format: date-time }
